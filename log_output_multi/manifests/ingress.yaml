apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: log-output-multi-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: myapps.local
      http:
        paths:
          - path: /status
            pathType: Prefix
            backend:
              service:
                name: log-output-multi-service
                port:
                  number: 3000
